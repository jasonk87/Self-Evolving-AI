# Default install set (safe for most local/dev environments)
-r requirements-core.txt
-r requirements-dev.txt

# Optional integrations are intentionally excluded from the default install
# to avoid native build failures (for example: pyaudio/portaudio).
# Install explicitly when needed:
#   pip install -r requirements-optional.txt
